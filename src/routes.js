import Landing from "./pages/Landing"

const routes = [
    {
        id : 1,
        htmlId : "page-landing",
        path : "/",
        exact : true,
        Element : Landing,
        private : false
    }
]

export default routes